# ╔═══════════════════════════════════════════════════════════════╗
# ║  HEADY SYSTEMS — Docker Desktop (noVNC) environment            ║
# ╚═══════════════════════════════════════════════════════════════╝

VNC_RESOLUTION=${VNC_RESOLUTION}
VNC_DEPTH=${VNC_DEPTH}
NOVNC_PORT=${NOVNC_PORT}
VNC_PORT=${VNC_PORT}

NODE_ENV=${NODE_ENV}
HEADY_TARGET=${HEADY_TARGET}
HEADY_VERSION=${HEADY_VERSION}
