# Event mesh

NATS JetStream is defined as the event backbone for notifications, analytics, scheduler releases, asset publishing receipts, and migration journals. The config file in `infra/nats/jetstream.conf` is ready for a local broker container or managed deployment adaptation.
