# Feature flags

Feature flags should sit at the gateway and app-surface level for support pages, billing plan visibility, migration rollout controls, and asset publishing channels. Keep definitions in code plus environment values until a dedicated control plane is introduced.
