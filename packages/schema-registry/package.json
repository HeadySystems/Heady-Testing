{
  "name": "@heady-ai/schema-registry",
  "version": "1.0.0",
  "description": "Central JSON Schema registry with validation for the Heady platform",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/",
    "build": "echo 'No build step required for pure JS package'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "keywords": ["schema", "registry", "validation", "json-schema", "heady"],
  "license": "UNLICENSED",
  "private": true
}
