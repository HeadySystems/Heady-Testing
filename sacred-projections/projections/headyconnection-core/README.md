# headyconnection-core

Target domain: `headyconnection.com`

This projection contains a static site shell for `HeadyConnection`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
