# headyapi-core

Target domain: `headyapi.com`

This projection contains a static site shell for `HeadyAPI`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
