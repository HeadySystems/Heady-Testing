# headyio-core

Target domain: `heady.io`

This projection contains a static site shell for `Heady IO`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
