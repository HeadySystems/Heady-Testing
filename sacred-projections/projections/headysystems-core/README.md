# headysystems-core

Target domain: `headysystems.com`

This projection contains a static site shell for `HeadySystems`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
