# headyme-core

Target domain: `headyme.com`

This projection contains a static site shell for `HeadyMe`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
