# headyos-core

Target domain: `headyos.com`

This projection contains a static site shell for `HeadyOS`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
