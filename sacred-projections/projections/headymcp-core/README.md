# headymcp-core

Target domain: `headymcp.com`

This projection contains a static site shell for `HeadyMCP`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
