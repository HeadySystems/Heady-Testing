# headybuddy-core

Target domain: `headybuddy.com`

This projection contains a static site shell for `HeadyBuddy`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
