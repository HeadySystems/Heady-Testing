# headybot-core

Target domain: `headybot.com`

This projection contains a static site shell for `HeadyBot`. Mount `site/` into the target repo and then wire live runtime panels back to the foundation modules.
