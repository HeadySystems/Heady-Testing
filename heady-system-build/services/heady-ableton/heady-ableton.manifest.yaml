# ──────────────────────────────────────────────────────────────────────────────
# heady-ableton — Kubernetes + Envoy Service Manifest
# Domain: headyme.com | Port: 3348
# CSL routing: domain matching via cosine similarity
# Concurrent-equals routing — phi-scaled CSL matching only
# ──────────────────────────────────────────────────────────────────────────────

apiVersion: v1
kind: Namespace
metadata:
  name: heady-system
  labels:
    app.kubernetes.io/part-of: heady-platform
    istio-injection: enabled

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: heady-ableton
  namespace: heady-system
  labels:
    app: heady-ableton
    heady.domain: headyme-com
    version: "3.2.3"
    app.kubernetes.io/part-of: heady-platform
spec:
  replicas: 3                   # F(4) = 3
  selector:
    matchLabels:
      app: heady-ableton
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge:        2        # F(3) = 2
      maxUnavailable:  1        # F(2) = 1
  template:
    metadata:
      labels:
        app: heady-ableton
        heady.domain: headyme-com
        version: "3.2.3"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port:   "9464"
        prometheus.io/path:   "/metrics"
    spec:
      serviceAccountName: heady-ableton-sa
      containers:
        - name: heady-ableton
          image: us-central1-docker.pkg.dev/heady-prod/heady-ableton:3.2.3
          imagePullPolicy: Always
          ports:
            - containerPort: 3348
              name: http
              protocol: TCP
            - containerPort: 9464
              name: metrics
              protocol: TCP
          env:
            - name:  NODE_ENV
              value: production
            - name:  SERVICE_NAME
              value: heady-ableton
            - name:  SERVICE_VERSION
              value: "3.2.3"
            - name:  HEADY_DOMAIN
              value: headyme.com
            - name:  PORT
              value: "3348"
            - name:  OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://heady-collector.heady-system.svc.cluster.local:4317"
            - name:  OTEL_SERVICE_NAME
              value: heady-ableton
            - name:  CSL_CONFIDENCE
              value: "0.618"
            - name:  CSL_COHERENCE
              value: "0.882"
            - name:  MTLS_ENABLED
              value: "true"
          envFrom:
            - secretRef:
                name: heady-ableton-secrets
                optional: true
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 7        # ≈ φ⁴ = 6.854s
            timeoutSeconds: 4       # ≈ φ³ = 4.236s
            failureThreshold: 3     # F(4) = 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 7
            timeoutSeconds: 4
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/startup
              port: http
            failureThreshold: 13    # F(7) = 13 (longer for slow-starting services)
            periodSeconds: 5        # F(5) = 5
          resources:
            requests:
              memory: "89Mi"        # F(11) = 89
              cpu:    "55m"         # F(10) = 55
            limits:
              memory: "377Mi"       # F(14) = 377
              cpu:    "233m"        # F(13) = 233

---
apiVersion: v1
kind: Service
metadata:
  name: heady-ableton
  namespace: heady-system
  labels:
    app: heady-ableton
    heady.domain: headyme-com
spec:
  selector:
    app: heady-ableton
  ports:
    - name: http
      protocol: TCP
      port: 3348
      targetPort: http
    - name: metrics
      protocol: TCP
      port: 9464
      targetPort: metrics
  type: ClusterIP

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: heady-ableton-sa
  namespace: heady-system

---
# PeerAuthentication — enforce mTLS for ALL traffic to this service
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: heady-ableton-peer-auth
  namespace: heady-system
spec:
  selector:
    matchLabels:
      app: heady-ableton
  mtls:
    mode: STRICT

---
# AuthorizationPolicy — zero-trust service-to-service rules
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: heady-ableton-authz
  namespace: heady-system
spec:
  selector:
    matchLabels:
      app: heady-ableton
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/heady-system/sa/heady-manager-sa"
              - "cluster.local/ns/heady-system/sa/heady-gateway-sa"
              - "cluster.local/ns/heady-system/sa/heady-ableton-sa"
      to:
        - operation:
            ports: ["3348"]

---
# HorizontalPodAutoscaler — Fibonacci-snapped replica counts
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: heady-ableton-hpa
  namespace: heady-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: heady-ableton
  minReplicas: 2    # F(3) = 2
  maxReplicas: 13   # F(7) = 13
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 62   # ≈ ψ × 100 = 61.8%
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 62

---
# VirtualService — CSL routing without ranking fields
# Traffic routing based on CSL domain header matching
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: heady-ableton-vs
  namespace: heady-system
spec:
  hosts:
    - heady-ableton.heady-system.svc.cluster.local
  http:
    - match:
        - headers:
            x-heady-domain:
              exact: headyme.com
        - headers:
            x-heady-service:
              exact: heady-ableton
      route:
        - destination:
            host: heady-ableton
            port:
              number: 3348
      timeout: 11s       # ≈ φ⁵ = 11.090s
      retries:
        attempts: 3      # F(4) = 3
        perTryTimeout: 4s
        retryOn: "5xx,reset,connect-failure,retriable-4xx"
    - route:
        - destination:
            host: heady-ableton
            port:
              number: 3348
