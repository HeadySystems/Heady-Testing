apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hcfullpipeline-executor
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "21"
    spec:
      containerConcurrency: 13
      containers:
        - image: gcr.io/${PROJECT_ID}/hcfullpipeline-executor:latest
          ports:
            - containerPort: 4314
          env:
            - name: PORT
              value: "4314"
            - name: HEADY_ENV
              value: "prod"
          startupProbe:
            httpGet:
              path: /health/live
              port: 4314
          livenessProbe:
            httpGet:
              path: /health/live
              port: 4314
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 4314
