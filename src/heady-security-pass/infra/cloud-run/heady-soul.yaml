apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: heady-soul
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "21"
    spec:
      containerConcurrency: 13
      containers:
        - image: gcr.io/${PROJECT_ID}/heady-soul:latest
          ports:
            - containerPort: 4308
          env:
            - name: PORT
              value: "4308"
            - name: HEADY_ENV
              value: "prod"
          startupProbe:
            httpGet:
              path: /health/live
              port: 4308
          livenessProbe:
            httpGet:
              path: /health/live
              port: 4308
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 4308
