# Analytics Service Configuration
PORT=3382
NODE_ENV=production

# PostgreSQL connection (optional — runs in-memory-only if not set)
# DATABASE_URL=postgresql://user:password@localhost:5432/heady_analytics
