apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: auto-success-engine
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "21"
    spec:
      containerConcurrency: 13
      containers:
        - image: gcr.io/${PROJECT_ID}/auto-success-engine:latest
          ports:
            - containerPort: 4315
          env:
            - name: PORT
              value: "4315"
            - name: HEADY_ENV
              value: "prod"
          startupProbe:
            httpGet:
              path: /health/live
              port: 4315
          livenessProbe:
            httpGet:
              path: /health/live
              port: 4315
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 4315
