apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: budget-tracker
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "21"
    spec:
      containerConcurrency: 13
      containers:
        - image: gcr.io/${PROJECT_ID}/budget-tracker:latest
          ports:
            - containerPort: 4303
          env:
            - name: PORT
              value: "4303"
            - name: HEADY_ENV
              value: "prod"
          startupProbe:
            httpGet:
              path: /health/live
              port: 4303
          livenessProbe:
            httpGet:
              path: /health/live
              port: 4303
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 4303
