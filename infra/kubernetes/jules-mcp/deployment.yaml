apiVersion: apps/v1
kind: Deployment
metadata:
  name: jules-mcp
  labels:
    app: jules-mcp
    heady.io/domain: mcp
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jules-mcp
  template:
    metadata:
      labels:
        app: jules-mcp
        heady.io/domain: mcp
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8804"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: jules-mcp
        image: gcr.io/gen-lang-client-0920560496/jules-mcp:latest
        ports:
        - containerPort: 8804
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "jules-mcp"
        - name: PORT
          value: "8804"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8804
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8804
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: jules-mcp
  labels:
    app: jules-mcp
    heady.io/domain: mcp
spec:
  selector:
    app: jules-mcp
  ports:
  - name: http
    port: 8804
    targetPort: 8804
