apiVersion: apps/v1
kind: Deployment
metadata:
  name: cli-service
  labels:
    app: cli-service
    heady.io/domain: cli
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cli-service
  template:
    metadata:
      labels:
        app: cli-service
        heady.io/domain: cli
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8904"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: cli-service
        image: gcr.io/gen-lang-client-0920560496/cli-service:latest
        ports:
        - containerPort: 8904
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "cli-service"
        - name: PORT
          value: "8904"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8904
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8904
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: cli-service
  labels:
    app: cli-service
    heady.io/domain: cli
spec:
  selector:
    app: cli-service
  ports:
  - name: http
    port: 8904
    targetPort: 8904
