apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  labels:
    app: mcp-server
    heady.io/domain: mcp
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-server
  template:
    metadata:
      labels:
        app: mcp-server
        heady.io/domain: mcp
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8800"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: mcp-server
        image: gcr.io/gen-lang-client-0920560496/mcp-server:latest
        ports:
        - containerPort: 8800
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "mcp-server"
        - name: PORT
          value: "8800"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8800
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8800
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  labels:
    app: mcp-server
    heady.io/domain: mcp
spec:
  selector:
    app: mcp-server
  ports:
  - name: http
    port: 8800
    targetPort: 8800
