apiVersion: apps/v1
kind: Deployment
metadata:
  name: hcfullpipeline-executor
  labels:
    app: hcfullpipeline-executor
    heady.io/domain: pipeline
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hcfullpipeline-executor
  template:
    metadata:
      labels:
        app: hcfullpipeline-executor
        heady.io/domain: pipeline
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8601"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: hcfullpipeline-executor
        image: gcr.io/gen-lang-client-0920560496/hcfullpipeline-executor:latest
        ports:
        - containerPort: 8601
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "hcfullpipeline-executor"
        - name: PORT
          value: "8601"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8601
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8601
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: hcfullpipeline-executor
  labels:
    app: hcfullpipeline-executor
    heady.io/domain: pipeline
spec:
  selector:
    app: hcfullpipeline-executor
  ports:
  - name: http
    port: 8601
    targetPort: 8601
