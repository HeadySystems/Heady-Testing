apiVersion: apps/v1
kind: Deployment
metadata:
  name: heady-vector
  labels:
    app: heady-vector
    heady.io/domain: vector
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heady-vector
  template:
    metadata:
      labels:
        app: heady-vector
        heady.io/domain: vector
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8107"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: heady-vector
        image: gcr.io/gen-lang-client-0920560496/heady-vector:latest
        ports:
        - containerPort: 8107
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "heady-vector"
        - name: PORT
          value: "8107"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8107
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8107
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: heady-vector
  labels:
    app: heady-vector
    heady.io/domain: vector
spec:
  selector:
    app: heady-vector
  ports:
  - name: http
    port: 8107
    targetPort: 8107
