apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: api-gateway
    heady.io/domain: gateway
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
        heady.io/domain: gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8701"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: api-gateway
        image: gcr.io/gen-lang-client-0920560496/api-gateway:latest
        ports:
        - containerPort: 8701
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "api-gateway"
        - name: PORT
          value: "8701"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8701
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8701
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: api-gateway
    heady.io/domain: gateway
spec:
  selector:
    app: api-gateway
  ports:
  - name: http
    port: 8701
    targetPort: 8701
