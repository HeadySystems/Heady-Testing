apiVersion: apps/v1
kind: Deployment
metadata:
  name: heady-embed
  labels:
    app: heady-embed
    heady.io/domain: embedding
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heady-embed
  template:
    metadata:
      labels:
        app: heady-embed
        heady.io/domain: embedding
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8105"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: heady-embed
        image: gcr.io/gen-lang-client-0920560496/heady-embed:latest
        ports:
        - containerPort: 8105
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "heady-embed"
        - name: PORT
          value: "8105"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8105
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8105
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: heady-embed
  labels:
    app: heady-embed
    heady.io/domain: embedding
spec:
  selector:
    app: heady-embed
  ports:
  - name: http
    port: 8105
    targetPort: 8105
