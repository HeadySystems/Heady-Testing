apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-mcp
  labels:
    app: memory-mcp
    heady.io/domain: mcp
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: memory-mcp
  template:
    metadata:
      labels:
        app: memory-mcp
        heady.io/domain: mcp
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8802"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: memory-mcp
        image: gcr.io/gen-lang-client-0920560496/memory-mcp:latest
        ports:
        - containerPort: 8802
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "memory-mcp"
        - name: PORT
          value: "8802"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8802
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8802
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: memory-mcp
  labels:
    app: memory-mcp
    heady.io/domain: mcp
spec:
  selector:
    app: memory-mcp
  ports:
  - name: http
    port: 8802
    targetPort: 8802
