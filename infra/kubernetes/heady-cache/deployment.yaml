apiVersion: apps/v1
kind: Deployment
metadata:
  name: heady-cache
  labels:
    app: heady-cache
    heady.io/domain: cache
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heady-cache
  template:
    metadata:
      labels:
        app: heady-cache
        heady.io/domain: cache
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8603"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: heady-cache
        image: gcr.io/gen-lang-client-0920560496/heady-cache:latest
        ports:
        - containerPort: 8603
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "heady-cache"
        - name: PORT
          value: "8603"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8603
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8603
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: heady-cache
  labels:
    app: heady-cache
    heady.io/domain: cache
spec:
  selector:
    app: heady-cache
  ports:
  - name: http
    port: 8603
    targetPort: 8603
