apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-tracker
  labels:
    app: budget-tracker
    heady.io/domain: finops
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: budget-tracker
  template:
    metadata:
      labels:
        app: budget-tracker
        heady.io/domain: finops
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8903"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: budget-tracker
        image: gcr.io/gen-lang-client-0920560496/budget-tracker:latest
        ports:
        - containerPort: 8903
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "budget-tracker"
        - name: PORT
          value: "8903"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8903
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8903
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: budget-tracker
  labels:
    app: budget-tracker
    heady.io/domain: finops
spec:
  selector:
    app: budget-tracker
  ports:
  - name: http
    port: 8903
    targetPort: 8903
