apiVersion: apps/v1
kind: Deployment
metadata:
  name: prompt-manager
  labels:
    app: prompt-manager
    heady.io/domain: prompts
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prompt-manager
  template:
    metadata:
      labels:
        app: prompt-manager
        heady.io/domain: prompts
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8905"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: prompt-manager
        image: gcr.io/gen-lang-client-0920560496/prompt-manager:latest
        ports:
        - containerPort: 8905
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "prompt-manager"
        - name: PORT
          value: "8905"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8905
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8905
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: prompt-manager
  labels:
    app: prompt-manager
    heady.io/domain: prompts
spec:
  selector:
    app: prompt-manager
  ports:
  - name: http
    port: 8905
    targetPort: 8905
