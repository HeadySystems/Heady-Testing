apiVersion: apps/v1
kind: Deployment
metadata:
  name: heady-maintenance
  labels:
    app: heady-maintenance
    heady.io/domain: reliability
    heady.io/autocontext: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heady-maintenance
  template:
    metadata:
      labels:
        app: heady-maintenance
        heady.io/domain: reliability
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8402"
    spec:
      terminationGracePeriodSeconds: 34
      containers:
      - name: heady-maintenance
        image: gcr.io/gen-lang-client-0920560496/heady-maintenance:latest
        ports:
        - containerPort: 8402
          name: http
        envFrom:
        - configMapRef:
            name: heady-global-config
        - secretRef:
            name: heady-secrets
        env:
        - name: SERVICE_NAME
          value: "heady-maintenance"
        - name: PORT
          value: "8402"
        - name: AUTOCONTEXT_URL
          value: "http://heady-auto-context:8907"
        - name: CONSUL_URL
          value: "http://consul:8500"
        - name: OTEL_ENDPOINT
          value: "http://otel-collector:4318"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8402
          initialDelaySeconds: 13
          periodSeconds: 21
          timeoutSeconds: 8
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8402
          initialDelaySeconds: 8
          periodSeconds: 13
          timeoutSeconds: 5
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: heady-maintenance
  labels:
    app: heady-maintenance
    heady.io/domain: reliability
spec:
  selector:
    app: heady-maintenance
  ports:
  - name: http
    port: 8402
    targetPort: 8402
